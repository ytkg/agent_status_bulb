# frozen_string_literal: true

RSpec.describe AgentStatusBulb do
  it "has a version number" do
    expect(AgentStatusBulb::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
